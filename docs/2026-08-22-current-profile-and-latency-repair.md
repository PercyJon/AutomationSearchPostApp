# 2026-08-22：当前用户主页与流程时延修复记录

本记录遵循项目根目录 `AGENTS.md` 的单轮修复与止损要求。

## 问题 A：评论私信的“当前用户主页”不执行

### 已报告的稳定复现步骤

1. 确认无障碍服务已开启，打开抖音并停留在一条普通视频（非直播、非广告）。
2. 切回本应用，进入“评论私信”，选择“当前用户主页”。
3. 保持默认的“视频数上限 = 1”和“每个视频用户数 = 1”，点击“立即开始”。
4. 应用切回抖音后，自动化没有任何动作。
5. 使用同一配置点击“挂起”；切回抖音后通过悬浮窗点击“恢复”，仍没有任何动作。

对照：同一设备上“搜索指定用户”路径可以正常运行。

### 基线状态

- 报告结果：立即开始与挂起后恢复均无动作。
- 真机立即开始复现（2026-08-22 17:11）：使用普通视频 `7676712767529479119`、当前用户主页、1 视频/1 用户。任务成功处理 1 名评论用户；这说明路径并非完全失效。
- 同次日志显示首次实际动作明显滞后：17:11:44 `comment_current_profile_entry_waiting`，17:11:53 首次 `OPEN_COMMENTS`，即任务切回抖音后约 9.5 秒才打开评论区。此前只观察短时间时，表象就是“没有任何反应”。
- 同次任务于 17:12:16 完成，记录为“已处理 1、跳过 0、失败 0”。
- 待采集：挂起后悬浮窗恢复的等价复现，以及首次页面上下文读取为何耗时较长。

### 修复轮次

#### A-1

- 根因假设：当前用户主页已通过“普通视频 + 已验证评论入口”结构确认可安全交接时，服务事件与主动轮询仍同步执行整屏 OCR；真机中这一步占用了约 8 秒，令首个动作看似没有发生。
- 最小修改：仅对上述已验证结构的当前用户主页入口跳过冗余 OCR；页面不满足该严格条件时，继续沿用现有 OCR 与风险/广告排除逻辑。
- 复现步骤：先重复立即开始的 1 视频/1 评论用户路径；挂起→恢复作为下一轮的完整共同回归项。
- 结果：改善。立即开始首个 `OPEN_COMMENTS` 由约 9.5 秒降至约 7.0 秒，任务成功完成 1 名用户；但整轮仍有不必要的后续 OCR，且首动作仍偏慢。

#### A-2

- 根因假设：当前用户主页的启动轮询在已完成安全交接后仍持续运行，并在评论区、评论用户主页和私信页触发整屏 OCR；这些竞争性读取延长了同一任务的后续步骤。
- 最小修改：一旦当前用户主页已经交接给评论运行时，立即结束该入口轮询；评论运行时自身的页面事件、步骤后置确认和超时保护保持不变。
- 复现步骤：重新执行 1 视频/1 评论用户的立即开始；再执行同配置的挂起→普通视频→悬浮窗恢复。
- 结果：无变化/变差。立即开始成功完成 1 名用户，但首个 `OPEN_COMMENTS` 在约 10.8 秒后才出现（基线约 9.5 秒）；挂起→恢复也成功完成 1 名用户，但“恢复”到首个 `OPEN_COMMENTS` 约 14.9 秒（基线约 12.6 秒）。交接完成后仍可见通用服务 OCR 日志，说明“入口轮询”不是主要剩余耗时来源。

### 止损：当前用户主页首动作延迟

本问题已连续完成两轮最小代码修改，仍未达到“切回抖音后应立即有动作”的目标；不进行第三次代码修改，等待确认。

1. A-1 做了什么：当当前用户主页已由严格普通视频评论入口结构确认时，跳过入口前的冗余 OCR。立即开始的单次首动作从约 9.5 秒改善为约 7.0 秒，但结果不稳定，后续复测回到约 10.8 秒。
2. A-2 做了什么：交接给评论运行时后停止当前用户主页入口轮询，避免该轮询继续发起 OCR。立即开始与挂起恢复均仍可见通用服务 OCR，首动作分别约 10.8 秒与 14.9 秒，未解决问题。
3. 日志与已排除原因：
   - 立即开始任务 `评论私信-20260822-172145`：17:21:46 启动，17:21:57 首次 `OPEN_COMMENTS`，最终“已处理 1、失败 0”。
   - 挂起恢复任务 `评论私信-20260822-172311`：17:23:43 点击恢复，17:23:58 首次 `OPEN_COMMENTS`，最终“已处理 1、失败 0”。
   - 两条路径都能正确将任务交接到普通视频并完成空格探测；无障碍服务在线，目标应用切换和悬浮窗恢复命令均未丢失。因此不是“当前用户主页”命令未下发、抖音未切回前台或评论运行时完全未启动。
   - 在恢复前的挂起阶段以及交接后的运行阶段，都记录到 `ocr_completed`；说明残余耗时来自通用未知页 OCR/页面读取链路，而非 A-2 已停止的入口轮询。
4. 偏离检查：未偏离。两轮都只针对“当前用户主页切回后首动作迟缓/看似无反应”的实际交接链路；没有改动搜索指定用户逻辑、评论候选顺序或私信发送逻辑。
5. 剩余假设（最多 3 个）：
   - 挂起阶段 `taskActive = true` 让通用服务对操作者手动导航的未知页面提前执行整屏 OCR，恢复前已经占用识别资源。
   - 评论运行时已具备明确阶段和节点信号后，通用服务仍会对 UNKNOWN 页面执行全屏 OCR，与评论区读取和私信页面确认竞争。
   - `currentWindowContext()` 的完整节点快照与服务事件的节点检查在同一转换期争用，导致已接收的普通视频事件延后交给状态机。
6. 下一步最小诊断动作（待确认后执行）：仅增加不改变行为的耗时埋点，分别测量 `currentWindowContext`、服务未知页 OCR 和 `onScreenObserved` 的耗时/排队间隔；然后按同一 1/1 两路径复测一次，以确定唯一主耗时点后再提出新的最小修复。

## 问题 B（P0）：整体流程等待时间偏长

### 覆盖路径

1. 搜索用户后等待用户列表；
2. 进入用户主页后等待打开视频；
3. 打开视频后等待打开评论区；
4. 评论区中进入评论用户主页。

### 基线状态

- 稳定性约束：仅缩短已由页面/节点状态确认可继续的等待；不得以固定更短延时替代必要页面确认。
- 当前用户主页普通视频的首段基线：约 9.5 秒后才发出打开评论区动作；评论区就绪后约 1.1 秒确认可读。
- 待采集：搜索用户、用户主页首视频、评论用户主页三段的同类起止时间与重试次数。

### 修复轮次

尚未开始代码修改。

## 问题 C：高评论量普通视频打开评论面板后超时

### 当前错误与稳定复现步骤（2026-08-22 17:36）

1. 在开发机 `b33aa309` 的抖音推荐页停留一条普通视频：右侧评论数显示为 `435`，没有直播或广告标识。
2. 切回本应用的“评论私信”，选择“当前用户主页”，配置“视频数上限 = 1”“每个视频用户数 = 7”，点击“立即开始”。
3. 自动化在 `17:36:15.849` 记录 `comment_open_panel` 的节点点击成功；随后连续 6 次 `comment_panel_postcondition` 均为 `ready=false`、`confidence=0.1`。
4. 任务在 `17:36:27.855` 以 `评论流程超时：等待评论区` 失败，候选用户数为 0，未发送或处理任何用户。

### 当前诊断状态

- 同页人工验证：对右侧真实评论气泡作坐标轻触后，评论面板立即打开。因此该视频本身和评论加载均正常。

### 修复轮次

#### C-1

- 根因假设：抖音对当前无障碍节点的 `ACTION_CLICK` 返回成功，但没有将动作派发到可视评论气泡；在两次面板就绪检查后，对当前重新识别且经紧凑右侧轨道安全校验的节点补一次坐标手势即可打开面板。
- 最小修改：仅在“打开评论区”节点点击后、第三次面板检查时增加一次受 `CommentPanelRecoveryPolicy` 约束的坐标重试；未通过该策略时只记录并不点击。
- 复现步骤：重复上面的同一条 435 评论普通视频、当前用户主页、1 视频/7 用户。
- 结果：无变化。任务 `17:41` 再次在等待评论区超时，处理 0 人。日志 `comment_panel_bounds_retry_suppressed` 证明重试没有执行任何坐标点击；安全边界（不点地址、AI 解析、点赞、评论内容）保持有效。

#### C-2（准备执行）

- 单一根因假设：评论入口的语义选择器接受了带“评论”文本的宽父容器；该容器的动作会被抖音接受但不会打开面板，同时又不满足紧凑右侧轨道的重试校验。实际评论气泡是同一轨道内更小的结构化子节点。
- 最小修改：让语义选择器只接受紧凑的右侧轨道节点；宽父容器交给既有的结构化轨道选择器处理。这样 C-1 的一次受限坐标重试可以只针对真实评论气泡。
- 复现步骤：仍使用同一条 435 评论普通视频和相同的当前用户主页 1×7 配置。
- 结果：无变化。`17:45:24.569` 仍记录 `comment_open_panel route=node_click success=true`；第三次检查后的 `comment_panel_bounds_retry_suppressed` 仍未执行手势；任务于 `17:45:36.579` 以“等待评论区”超时，处理 0 人。

### 止损：当前普通视频评论入口未打开

同一问题已完成两轮最小代码修改且均未解决，**不进行第 3 次代码修改，等待确认**。

1. C-1 做了什么：节点点击后经过两次未就绪检查，尝试对“当前重新识别且通过紧凑右侧轨道安全策略”的评论节点发起一次坐标手势。结果：策略安全地拒绝了目标，未产生坐标点击，任务仍超时。
2. C-2 做了什么：收紧“评论”语义节点的几何条件，拒绝宽父容器并让现有结构化轨道选择实际第二个评论控制；同时新增单元测试保护该选择规则。结果：任务仍报告 `node_click`，补偿手势仍被抑制，未改善。
3. 日志、报错与已排除原因：
   - C-1：`17:41:26.766 comment_open_panel route=node_click success=true`；`17:41:30.841 comment_panel_bounds_retry_suppressed`；`17:41:38.773 comment_runtime_terminal outcome=FAILED candidate_count=0`。
   - C-2：`17:45:24.569 comment_open_panel route=node_click success=true`；`17:45:28.616 comment_panel_bounds_retry_suppressed`；`17:45:36.579 comment_runtime_terminal outcome=FAILED candidate_count=0`。
   - 在同一可见页面，人工对真实右侧评论气泡执行一次坐标轻触，评论面板立即打开并显示“评论 435”。因此已排除：视频是直播/广告、视频没有评论、评论面板自身加载失败、无障碍服务完全失效。
   - 两轮均未触发地址、AI 解析、点赞、收藏、评论内容或用户行的点击；补偿手势日志明确为 `suppressed`。
4. 偏离检查：未偏离。两轮只修改评论入口选择/后置确认路径；没有改动搜索指定用户、评论首屏排序、私信探测或真实消息发送逻辑。
5. 剩余假设（最多 3 个）：
   - `withLiveNode` 在重新解析层级路径时命中了与快照不同的无效父节点；`node_click` 的成功值不等于视觉入口已触发。
   - 当前用户主页快速交接路径的无障碍树与手工可见树在评论气泡处存在坐标/层级偏移，导致快照选择与真实可点气泡不一致。
   - 页面刚切回抖音时的通用 OCR/页面探测竞争使 `currentContext()` 在补偿阶段获取到过期或不完整树，因而无法重新确认紧凑右侧节点。
6. 下一步最小诊断动作（待确认后执行）：仅增加不改变任何 UI 行为的目标快照与实时重定位埋点（节点类型、语义、边界、层级路径、是否能通过安全策略），对同一 435 评论视频复现一次；据此只保留一个可验证根因后再提出下一次最小修复。

## 问题 D：任务 `评论私信-20260822-175116` 从私信返回评论区时误判失败

### 当前错误与稳定复现步骤

1. 在 `b33aa309` 的抖音普通视频页启动“当前用户主页”评论私信任务；该任务配置为 1 个视频、每视频 3 个评论用户。
2. 首位评论用户已完成私信空格探测：`17:51:44.190` 提交空格，`17:51:45.031` 记录 `BLANK_PROBE_VERIFIED`。
3. 自动化从私信页依次返回用户主页，再返回评论区。`17:51:49.937` 已收到评论区观察事件，日志为 `surface_ready=true`，并将该视图合并进评论读取队列。
4. 返回循环随后读取到一个缺少面板标记的较晚快照，在 `17:51:51.559` 将评论区判为 `surface=false`，继续执行恢复；最终 `17:51:53.374` 以“无法在限定次数内返回评论区”失败，任务仅处理 1 人。

### 基线与单一根因假设

- 基线：评论面板已由无障碍事件确认，但 `returnToCommentSurface()` 只读取 `currentContext()`；在 RecyclerView/面板重绘期间，这个即时树可能短暂丢失页头与回复标记，覆盖掉先前已经确认的面板事实。
- 单一根因假设：返回循环没有优先消费最新的、已确认的评论区无障碍观察，因此把真实已恢复的评论区误判为未恢复。

### 修复轮次

#### D-1

- 最小修改：仅在返回评论区期间保留通过 `isCommentSurfaceReady` 的事件本身；每个返回循环开始时优先消费这份事件确认，避免被随后不完整的即时树覆盖。
- 复现步骤：重复当前用户主页、1 视频/3 用户的同一普通视频闭环，验证首个用户后继续回到评论区并依序处理后续用户。
- 结果：改善但未解决。18:00 复测中，首位用户空格探测成功后，`18:00:49.875` 已记录 `comment_return_surface_event_confirmed`，并开始了第 2 个队列用户；因此不再以“无法在限定次数内返回评论区”终止。但是该确认发生在当前循环起始检查之后，循环仍在 `18:00:50.934` 的同一轮末尾多执行了一次 BACK，把实际已恢复的评论区关闭。第 2 位用户随即因无法从已关闭面板重新定位头像而记录 `IDENTITY_UNAVAILABLE`。任务最后虽完成，但 3 位中只有第 1、3 位完成空格探测，不能视为修复完成。

#### D-2

- 当前错误与稳定复现：沿用 D-1 的同一普通视频和 1 视频/3 用户配置；首位用户返回时，评论区确认事件在循环读取即时树期间异步到达，随后仍发送了本不应发生的 BACK。
- 单一根因假设：确认事件已被保存，但返回循环只在一轮开头读取它；事件若在 OCR/当前窗口读取之间到达，发送 BACK 前没有第二次栅栏检查。
- 最小修改：只在调用 `GLOBAL_ACTION_BACK` 之前增加一次相同的确认检查；一旦事件已确认评论区，立即返回成功，不再点击、滚动或发送 BACK。
- 复现步骤：重复同一当前用户主页、同一普通视频、1 视频/3 用户闭环；预期首位用户后保持评论面板，按冻结的首屏顺序继续处理第 2、3 位。
- 结果：未解决。任务 `评论私信-20260822-180423` 于 `18:04:39` 启动，首位用户仍在 `18:05:03.407` 完成 `BLANK_PROBE_VERIFIED`。随后依次记录私信页、用户主页和视频页的返回观察；`18:05:09.963` 只检测到 OCR 标记“评论、回复”，`isCommentSurface=false`，且 `comment_reopen_ocr_suppressed` 证明没有对 OCR 坐标执行点击。此轮没有出现可消费的 `comment_return_surface_event_confirmed`，最终 `18:05:11.739` 仍以“无法在限定次数内返回评论区”失败，处理 1 人。

### 当前状态：止损阈值已调整，继续 D-3

用户已将 `AGENTS.md` 的止损阈值从“连续两轮、禁止第 3 次修改”调整为“连续五轮、禁止第 6 次修改”。D-1、D-2 均未形成可稳定完成 3 人闭环的结果，但允许继续执行 D-3。

1. D-1 做了什么：在共享评论运行时的返回阶段保存已确认的评论面板事件，并在每轮返回开始前优先消费。结果：改善，测试 `评论私信-20260822-180001` 不再在首人后立刻终止，已进入第 2 个队列用户；但确认事件在本轮检查后抵达，随后仍多执行了一次 BACK，评论面板被关闭。
2. D-2 做了什么：仅在 `GLOBAL_ACTION_BACK` 前增加第二次相同事件确认栅栏。结果：无改善，测试 `评论私信-20260822-180423` 中没有到达可用的评论面板事件，仍在首人后失败。
3. 日志、报错与已排除原因：
   - D-1：`18:00:49.875 comment_return_surface_event_confirmed`，`18:00:51.388 comment_surface_restored`，随后第 2 位用户无法在已关闭面板中重定位头像。
   - D-2：首位用户探测成功；`18:05:09.963 comment_return_to_surface_attempt page=UNKNOWN surface=false`，`18:05:09.964 comment_reopen_ocr_suppressed`，`18:05:11.739 comment_task_failed`。
   - 已排除：当前用户主页进入普通视频、打开评论区、冻结首屏候选、首位头像进入主页、纸飞机私信入口、空格探测及空消息拒绝提示均正常；这不是搜索入口、真实消息发送或首位评论用户选择的问题。
   - 两轮均未点击地址、AI 解析、点赞、收藏、评论内容或 OCR 文本坐标；D-2 的恢复动作明确被安全策略抑制。
4. 偏离检查：未偏离。改动都位于搜索指定用户与当前用户主页共同复用的 `CommentPrivateMessageRuntime` 返回评论区路径；两种入口在“已确认普通视频”之后本就共用该运行时，没有新建当前用户主页专属的评论/私信分支。
5. 剩余假设（最多 3 个）：
   - 评论面板在第二次 BACK 后实际出现，但抖音没有发送完整面板事件，且即时树只留下 OCR 文本，导致严格 `CommentSurfaceDetector` 无法确认它。
   - 视频右侧的真实评论气泡在返回瞬间未暴露为可安全点击的无障碍紧凑节点；当前仅见 OCR 备选，因此恢复策略按安全约束拒绝手势。
   - 评论面板事件与根树重建存在设备相关的事件排序问题，导致运行时收到的有效面板事件被更早/更晚的不完整树替代，需先区分“没有事件”与“事件被丢弃”。
6. D-3 的只读现场诊断已完成：从 `18:05:07` 的运行时截图可见，第二次 BACK 后评论面板已经实际打开（左下抽屉、`评论 1210`、`AI 解析`、多行评论、多个“回复”和底部评论输入框均可见）；但对应上下文只被识别为“OCR markers: 评论, 回复|repeated reply markers”。因此可以排除“评论面板未出现”，且无需点击任何恢复入口。

#### D-3

- 当前错误与稳定复现：同一条普通视频、当前用户主页、1 视频/3 用户。首位用户完成空格探测并依次 BACK 到视频页后，截图已经显示评论面板，`CommentSurfaceDetector` 却因无障碍树稀疏、OCR 未保留完整标题/AI/输入框文本而返回 `isCommentSurface=false`。
- 单一根因假设：评论面板检测器只接受“精确标题/输入框/结构化头像行”等强信号；在本机该真实面板的 OCR 降级形态中，唯一稳定信号是抽屉左侧的“评论”标题和其下方至少两行左侧“回复”行，故被错误拒绝。
- 最小修改：仅向 `CommentSurfaceDetector` 增加一个几何受限的 OCR 面板规则：左半屏中段的“评论”标题，且其下方存在至少两个左侧/中部“回复”行时确认已打开评论面板。该规则只影响“是否已经返回评论区”的判定，不产生任何点击。
- 复现步骤：重复相同 1×3 真机闭环；预期首位用户返回后直接保留评论面板，继续按已冻结顺序处理第 2、3 位。
- 结果：未解决且发现安全回归。首位用户在 `18:15:09` 完成空格探测；`18:15:10.919` 的 `comment_return_surface_event_confirmed` 在第一次 BACK 后到达，但 `18:15:11.843` 的即时上下文仍是 `USER_PROFILE`。返回循环仍把这个与最终返回阶段无关联的缓存事件视为成功，提前开始第 2 位用户，后者因实际仍不在评论区而记录 `IDENTITY_UNAVAILABLE`。之后 `18:15:22` 的恢复分支将一个误识别的紧凑节点交给通用点击函数；节点 ACTION_CLICK 失败后，该函数降级为 `bounds_gesture`，误点了首条评论右下的心形。任务已停止，且在任务不运行时已将该心形恢复为未点赞、计数由 706 回到 705。此轮不再允许该返回分支触发任何自动点击。

#### D-4（已完成）

- 当前错误与稳定复现：沿用同一普通视频、当前用户主页、1 视频/3 用户。首位用户完成空格探测后，从私信页 BACK 到用户主页时，旧评论区事件仍可能迟到；随后返回循环会在尚未抵达评论区时提前开始下一人，或在模糊页面上尝试恢复点击。
- 单一根因假设：`returnToCommentSurface()` 将“曾经收到评论区事件”当成当前已回到评论区的充分条件，未把该事件绑定到最终 BACK 阶段和一份非嵌套的当前页面确认；同一未确认状态又允许恢复分支执行动作，因而既会提前推进队列，也会对错误控件产生副作用。
- 最小修改：缓存事件只能在本次返回已完成所需 BACK 阶段、且新鲜上下文已确认不在私信/用户主页等嵌套页时才可作为补充证明；返回阶段不再自动重开评论区或执行任何点击，只允许检测、等待和必要的 BACK。这样即使检测仍不充分，也会安全失败而不会关闭面板、点 AI 解析或点赞。
- 复现步骤：重复相同 1×3 真机闭环；验收为首位之后只能在真实评论区按顺序继续处理，且全过程没有 `comment_reopen_panel`、`bounds_gesture`、点赞、地址、AI 解析或评论内容点击。
- 结果：改善并解决本问题。任务 `评论私信-20260822-183003` 在 `18:30:38` 以当前用户主页、1 视频/3 用户启动，并于 `18:31:43` 完成，界面记录为“已处理 3、跳过 0、失败 0”。三位均完成 `BLANK_PROBE_VERIFIED`；每位返回均记录 `DIRECT_MESSAGE`（BACK 1）→ `USER_PROFILE`（BACK 2）→ `accessibility_event_after_final_back`，之后才开始下一位。三次最终观察仍是稀疏 OCR 形态，但已由同一轮 `back_actions=2` 的事件可靠确认。日志中没有 `comment_reopen_panel`、`bounds_gesture`、评论心形、地址、AI 解析或评论正文点击；首条评论在任务开始前和首次回屏时均保持未点赞、计数 705。D-4 至此结束，未触发止损。
- 扩展回归：任务 `评论私信-20260822-183343` 在同一普通视频以当前用户主页、1 视频/7 用户运行，`18:34:03` 启动、`18:36:20` 完成，界面记录“已处理 7、跳过 0、失败 0”。先冻结首屏 4 人（行位置 1170、1539、1786、2155）并逐个处理；仅在第 4 人完成且评论区已确认返回后滚动一次，新增冻结 3 人（1292、1627、1959）。7 位均 `BLANK_PROBE_VERIFIED`，没有 `comment_reopen_panel`、`bounds_gesture`、点赞、地址或 AI 解析动作；任务正常回到 App。
- 挂起/恢复回归：任务 `评论私信-20260822-183806` 先以当前用户主页、1 视频/5 用户创建并挂起；切换到抖音的一条普通视频后，通过悬浮窗点击“恢复”。恢复探测接受当前普通视频并在 `18:39:43` 打开评论区，5 位评论用户依次完成 `BLANK_PROBE_VERIFIED`，`18:41:13` 正常完成。最终 App 记录为“已处理 5、跳过 0、失败 0”。每位都按私信页→用户主页→评论区的两次 BACK 返回；日志未出现 `comment_reopen_panel`、`bounds_gesture`、点赞、地址或 AI 解析动作，任务正常回到 App。
