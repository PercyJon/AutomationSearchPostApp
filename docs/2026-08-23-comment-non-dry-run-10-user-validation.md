# 2026-08-23：评论私信非干跑 10 用户验证

本次为用户明确授权的真实场景空消息安全闭环验证，遵循项目根目录 `AGENTS.md`。

## 测试条件

- 搜索词：`designer`
- 匹配词：“不错”与“漂亮”
- 匹配方式：任一匹配（未指定方式时采用当前默认）
- 视频数：1
- 每视频用户上限：10
- 执行方式：非干跑空消息安全探测

## 预期

对每一位经过头像、正文、页面状态验证的匹配评论用户，进入用户主页和私信页，提交单个空格并确认平台拒绝；不发送真实内容。任何结构、风控或页面状态不确定均应停止或跳过，不得使用坐标兜底。

## 实际

- 真机：OnePlus NE2210（Android 16 / SDK 36，设备序列号 `b33aa309`）。
- 运行时确认：`dry_run=false`、`terms_count=2`、`per_video_cap=10`。
- 结果：识别并处理 10 位不同的命中评论用户；处理 10、跳过 0、失败 0；任务以“已完成当前视频的评论用户上限探测”完成。
- 每位用户均按同一安全路径完成：验证评论头像 → 用户主页 → 私信页 → 提交单个空格 → 收到平台拒绝空消息提示 → 两次受控返回评论区。
- 未发送真实内容；任务记录仅保留“空消息模拟（空格）”及平台拒绝结果。
- 首次下发时，设备 Shell 将未转义的词分隔符误解释为管道。该异常创建的两条记录均为处理 0、跳过 0、失败 0，未进入评论用户主页；它们不计入本次有效验证。随后以安全转义重试并确认参数后，才开始本次有效运行。
