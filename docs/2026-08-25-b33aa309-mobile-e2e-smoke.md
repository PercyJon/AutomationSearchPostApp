# b33aa309 Android 真机 E2E 冒烟记录

## 开始前记录

- 当前目标：在不启动抖音、不执行自动化任务的前提下，验证当前 Debug APK 的登录会话、B 端任务创建界面、评论任务创建界面、待办、任务记录和“我的”服务状态路径。
- 设备：OnePlus NE2210（序列号 `b33aa309`，Android 16，1080×2412）。
- 稳定复现路径：系统桌面启动 App，依次进入首页、B 端私信、评论私信、待办、任务记录和“我的”。
- 预期行为：已有移动端会话可恢复；表单、保存前校验、返回和列表导航正常；不点击“立即开始”，不打开抖音，不发送消息。
- 实际基线：设备在线，已安装 `0.3.4-mobile-login` Debug APK；App 当前处于系统桌面。Android 工作区仅存在用户原有的删除/未跟踪交接文件，未包含本轮实现改动。
- 本轮唯一假设：当前 Android UI、登录会话和本地任务导航可在同一真机连续完成非执行型路径验证，且不依赖未完成的后端单设备解绑功能。
- 修改边界：仅安装当前 Debug APK、执行真机界面验证并记录结果；不修改自动化运行逻辑、不保存或启动任务、不操作抖音。

## 验证计划

1. 重新构建并以受管 HTTPS 服务地址安装当前 Debug APK，不清除 App 数据。
2. 检查登录会话与“我的”页授权/服务状态。
3. 检查 B 端与评论私信表单的打开、必填校验和返回路径；检查待办与任务记录入口。
4. 运行静态检查、单元测试、lint 与 Debug 构建，结束后停止 App 并回到系统桌面。

## 实施结果

- 结果：**通过**。当前 Debug APK 在不清除数据的覆盖安装后可恢复移动端会话，首页、账户设置、待办、任务记录和两张任务表单的非执行型路径均连续可达。
- 账户设置中执行一次“立即验证 heartbeat”后，页面持续显示“已验证 / 授权有效”，未出现授权或网络错误提示。
- B 端表单验证了标题、横向字段、预设词与空搜索条件的校验提示；测试期间发现上一次视觉验证留下的临时预设选择，已取消选择且未保存任务。
- 评论表单验证了指定用户入口、匹配模式选择及系统返回；“开始位置 / 当前用户主页 / 搜索指定用户 / 任务配置”均未重新出现。
- 全程未进入无障碍服务设置、未保存/挂起/启动任务、未打开抖音、未发送消息。

## 验证结果

- 覆盖安装当前 Debug APK 成功，未清除 App 数据；冷启动直接恢复首页会话。
- **真机 OnePlus NE2210 / b33aa309 / Android 16**：
  1. 首页 → 我的：显示“已授权 / 授权有效”及“自动化服务 / 未开启”。
  2. 我的 → 账号与设备：显示已验证状态；触发一次 heartbeat 校验后状态保持有效；系统返回正确回“我的”。
  3. 我的 → 任务记录：显示空态“暂无任务记录”；系统返回正确回“我的”。
  4. 待办：显示空态“暂无待办任务”，未点击“开始任务”。
  5. 首页 → B 端私信：显示“新建 B 端获客任务”；取消临时预设后出现搜索词校验提示，随后系统返回正确回首页。
  6. 首页 → 评论私信：显示“新建评论区私信任务”；“全部匹配”可切换为选中态，随后恢复“任一匹配”并系统返回首页。
- 测试结束时首页统计仍为 0 个任务、0 个已发送客户，App 已停止并返回系统桌面。

## 几何与安全检查

- 本阶段未修改 Android 业务或界面代码；现有表单几何仍只使用 Compose `dp`。未新增固定 px、自动化坐标、OCR/图像匹配逻辑或手势时长。
- 真机操作仅限应用内导航、表单选择和授权 heartbeat；没有触发抖音、自动化执行或消息发送。
