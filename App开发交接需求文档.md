已整理为可直接交给新会话/智能体继续执行的交接文档；未包含账号、数据库密码或 Token 等敏感信息。

# 抖音自动化营销 App：开发交接需求文档

## 1. 项目概述

项目为 Android 自动化营销客户端，技术栈为 Kotlin + Jetpack Compose + AccessibilityService，最低支持 Android API 30。

核心目标分为两条自动化链路：

1. B 端私信：搜索关键词 → 用户列表 → 用户主页 → 私信页 → 安全模拟发送。
2. 评论私信：指定用户主页/当前主页 → 视频 → 评论区 → 筛选评论用户 → 用户主页 → 私信页 → 安全模拟发送。

当前最高优先级是：

> P0：先跑通一次“评论私信”的真实、安全、有限闭环，不做大规模执行。

当前 P0 固定测试参数：

- 搜索目标：`designer`
- 进入“用户”标签后的第一个用户
- 仅处理 1 个视频
- 仅处理评论区顶部第一条有效评论
- 不使用评论关键词过滤
- 不跳过置顶视频
- 私信仅执行“空格安全探测”，不发送真实文案
- 成功判定：抖音出现“不能发送空白消息”等等价提示
- 完成后返回评论区，不继续无限滚动或处理大量评论

## 2. 当前代码与运行环境

### 2.1 主要工程路径

实际 Android 工程此前位于：

```
/Users/mac/Documents/Codex/2026-08-17/referenced-chatgpt-conversation-this-is-an
```

当前终端工作目录可能不同，新智能体应先使用文件搜索确认工程位置。

Android 包名：

```
com.example.douyinautomation
```

抖音包名：

```
com.ss.android.ugc.aweme
```

真机序列号：

```
b33aa309
```

无障碍服务组件：

```
com.example.douyinautomation/com.example.douyinautomation.automation.DouyinAccessibilityService
```

### 2.2 重要代码位置

| 文件                                                         | 作用                                                 |
| ------------------------------------------------------------ | ---------------------------------------------------- |
| `app/src/main/java/com/example/douyinautomation/automation/DouyinNavigationController.kt` | B 端私信主状态机、启动恢复、超时、页面导航           |
| `app/src/main/java/com/example/douyinautomation/automation/CommentPrivateMessageRuntime.kt` | 评论私信运行时状态机                                 |
| `app/src/main/java/com/example/douyinautomation/automation/CommentCandidateExtractor.kt` | 评论候选项识别与过滤                                 |
| `app/src/main/java/com/example/douyinautomation/automation/DouyinAccessibilityService.kt` | 无障碍事件、节点根节点获取、任务指令接收             |
| `app/src/main/java/com/example/douyinautomation/automation/PageDetector.kt` | 抖音页面类型识别                                     |
| `app/src/main/java/com/example/douyinautomation/ui/HomeScreen.kt` | 首页、待办、B 端私信表单、评论私信表单及 P0 默认参数 |
| `app/src/main/java/com/example/douyinautomation/MainActivity.kt` | 调试入口、深链/Intent 入口                           |
| `docs/M3.5-comment-private-message-foundation.md`            | 评论私信的基础设计和 P0 回归约束                     |

### 2.3 当前构建命令

```
./gradlew :app:testDebugUnitTest :app:lintDebug :app:assembleDebug
```

安装：

```
adb -s b33aa309 install -r app/build/outputs/apk/debug/app-debug.apk
```

开发期间检查无障碍状态：

```
adb -s b33aa309 shell dumpsys accessibility
```

开发期可使用 ADB 启用已授权的无障碍服务；正式产品必须仍由用户手动在系统设置授权。

不要在开发中依赖 `uiautomator dump`，此前它曾导致无障碍服务异常断开。优先使用 Accessibility 节点、应用日志和必要的设备截图。

## 3. 总体开发原则

### 3.1 自动化识别优先级

统一遵循：

```
Accessibility Node
→ 节点结构 / Bounds / 文本语义
→ 局部 OCR 辅助
→ OpenCV 模板识别
→ 归一化坐标兜底
```

必须坚持：

- 关键按钮“节点优先，OCR 不直接点击”。
- OCR 只用于辅助判断、补全文字或异常页面，不应成为主流程的每步依赖。
- 坐标只能作为最后兜底，不能成为核心逻辑。
- 不实现验证码破解、平台风控绕过、规避安全机制或规避检测逻辑。
- 遇到验证码、明显风控、系统拦截，应暂停或标记失败并交由人工处理。
- 任务执行的目标是稳定连续，不追求“每一个用户都必须处理成功”；超时、无法识别或无法私信的用户应记录并跳过。

### 3.2 运行与测试原则

- 每个自动化步骤都应有：进入条件、成功确认、超时、恢复动作、失败记录。
- 开发期操作延迟应尽量短，只保留页面真实加载所需的等待。
- 测试时不要无限滚动评论区或用户列表。
- 评论区测试最多处理 5、10 或 20 条，按任务上限停止。
- 只有发现异常时才截图、OCR 或抓取详细诊断，不应每一步截图。
- 真机测试结束后，应回到 App 的“记录”页或任务详情页，体现任务完成状态。

## 4. 当前最高优先级：P0 评论私信真实闭环

### 4.1 P0 验收目标

在真实抖音环境内，完成如下安全闭环一次：

```
抖音首页
→ 搜索 designer
→ 用户标签
→ 第一个用户
→ 用户主页
→ 第一个视频
→ 打开评论区
→ 第一条有效评论的头像
→ 评论用户主页
→ 发私信
→ 输入一个 ASCII 空格
→ 点击发送
→ 检测“不能发送空白消息”
→ 后退两层
→ 回到原视频评论区
→ 记录为“模拟发送成功”
```

不发送真实私信内容。

### 4.2 启动前的页面归一化规则

抖音被调起时可能停留在：

- 用户主页；
- 搜索页；
- 私信页；
- 视频页；
- 开屏广告；
- 直播预览页；
- 已进入直播间；
- 上次测试残留页面。

正确规则：

1. 若当前不是抖音首页，优先触发全局返回。
2. 重复返回，直到识别为抖音首页。
3. 最多允许有限次数，例如 4 次。
4. 若无障碍暂时取不到节点树，也应执行有限次数的“盲后退”，不能直接卡死。
5. 回到首页后，才允许点击搜索按钮并输入 `designer`。
6. 超出恢复次数仍无法回首页，记录失败并返回 App，不要无限尝试。

当前 `DouyinNavigationController.kt` 已开始实现这一方向，但仍有未完成缺口：

- `scheduleInitialObservation()` 在没有读到节点树时仍可能直接结束观察。
- 应改为调用“有限盲后退恢复”逻辑，而不是 `return`。
- 初始页面任何非首页状态都应统一归一化，不能复用旧搜索页或旧用户列表。
- 需要在真实设备上验证从“上次停留在用户主页”的情况下能正确回首页后再搜索。

### 4.3 P0 调试默认数据

目前已尝试在 UI 中预置：

```
搜索词：designer
模式：搜索指定用户
最大视频数：1
最大处理用户：1
评论匹配词：空
跳过置顶：否
发送模式：空消息模拟
```

新智能体需要确保：

- 点击“立即开始”后直接启动，不需要手动重新填写。
- 不应依赖 UI 中过时的 `serviceConnected` 状态造成自动启动失效。
- 当前存在“调试 Intent 已进入 App，但任务没有真正下发到无障碍服务”的启动竞态问题。
- 应在 `HomeScreen.kt` 的 `autoStartP0` 逻辑中等待服务真正连接后发送任务，或改为更可靠的服务侧调试任务启动入口。
- 成功启动时日志必须出现明确的任务启动记录，例如 `poc_started`、`comment_task_started` 等。

### 4.4 评论区有效评论的严格定义

必须忽略：

- 顶部定位卡片：左侧地址图标 + “天津市xxx”等地点文本；
- “xxx条评论”评论总数；
- “期待你的评论”“去评论”空评论提示；
- 时间、地区、回复、点赞、踩、已选中等控制元素；
- 点赞/踩/收藏/转发等图标文字；
- 视频标题、地址、搜索词、顶部控制栏文字。

一条有效评论必须满足：

```
左侧：圆形用户头像
右侧上方：灰色、小字体的评论用户名
右侧中部：黑色、较大字体的评论正文
右侧下方：时间 / 回复 / 喜欢 / 不喜欢等元信息
```

点击规则：

- 只点击该评论左侧头像的中心区域。
- 禁止点击评论文本、用户名以外的区域、地址、时间、回复、点赞、踩或视频控制区。
- 如果无法可靠识别头像与该条评论结构的绑定关系，宁可跳过该条评论并记录失败。
- P0 只处理评论区中第一条有效评论，不应下滑去点首屏最后一个评论。

### 4.5 评论用户主页后的逻辑

评论用户头像点击后：

1. 等待用户主页确认。
2. 识别私信入口，必须是“发私信”文字按钮或纸飞机私信按钮。
3. 不得误点“咨询客服”、商品、购物车、外链按钮等。
4. 进入私信页后，输入 ASCII 空格。
5. 点击发送。
6. 出现“不能发送空白消息”等提示，记为“模拟发送成功”。
7. 若出现权限限制、发送失败、超时、找不到输入框等，记为失败并跳过。
8. 完成后必须：
   - 从私信页返回评论用户主页；
   - 再返回原视频评论区；
   - 验证评论区仍在，才算闭环完成。

## 5. 评论私信正式功能需求

### 5.1 两种启动分支

#### A. 搜索指定用户

```
App 评论私信任务表单
→ 输入搜索词
→ 搜索抖音用户
→ 进入指定用户主页
→ 从第一个视频开始
→ 评论区私信
```

#### B. 当前用户主页模式

```
用户已手动打开抖音某个用户主页
→ 在悬浮窗中点击开始
→ 从该用户的第一个视频开始
→ 评论区私信
```

### 5.2 视频与用户主页兼容

- 用户主页可能默认展示“橱窗”“商品”“日常”等标签，而不是作品。
- 若页面存在多个标签：
  - 识别“作品”标签是否已选中；
  - 已选中条件：作品文字深色、下划线存在、必要时下箭头状态正确；
  - 只有未选中时才点击“作品”。
- 如果用户主页只有一个“作品 N”标签：
  - 不要点击该标签；
  - 直接点击下方第一个视频。
  - 点击单一作品标签会弹出“最新/最热”菜单，属于无效操作。
- 无作品：识别“暂无作品”等文本，直接跳过。
- 私密账号：识别“私密账号”等文本，直接跳过。
- 置顶视频：
  - 任务表单需保留“是否跳过置顶视频”开关；
  - 开启时跳过“置顶”标识的视频，选择第一个非置顶视频；
  - P0 测试当前不跳过置顶。

### 5.3 直播兼容

存在两种直播状态：

1. 未进入直播间，但视频页有“点击进入直播间”等按钮。
   - 识别该提示后直接向上滑动切走，不进入直播间。
2. 已进入直播间。
   - 通过右上角关闭 `X`、右下角转发按钮、直播间典型元素识别。
   - 优先执行返回事件或点击右上角关闭按钮。
   - 回到直播预览页后再向上滑动切走。
   - 不在直播间内执行评论私信。

### 5.4 评论筛选条件

- 评论关键词筛选为可选。
- 不启用筛选：处理所有符合结构的评论用户。
- 启用筛选：只有评论正文包含任一关键词时才进入其主页。
- 预设词与自定义词支持多个，使用 `|` 分隔。
- 关键词匹配应作用于评论正文，而不是用户名、地点、时间或控制文字。

### 5.5 悬浮窗

需要支持：

- 仅在任务运行且离开 App 后显示；
- 展示：
  - 当前任务名；
  - 已处理 / 任务上限；
  - 成功数 / 失败数 / 总数；
  - 暂停；
  - 停止。
- 支持右上角“收起”；
- 收起后显示为贴边圆角小方块；
- 点击小方块恢复完整悬浮窗；
- 正式用户必须在系统设置授予悬浮窗权限；
- 若权限未授予，应明确提示而不是静默失败。

## 6. B 端私信模块：已有功能与待收尾项

### 6.1 已实现或已部分实现

- 搜索关键词；
- 切换到用户标签；
- 用户列表识别；
- 进入用户主页时优先点击名称/信息区域，不点头像，避免进入直播；
- 私信入口识别；
- 空格模拟发送；
- “不能发送空白消息”作为安全探测成功；
- 私信权限限制与发送失败识别；
- 失败后跳过当前用户；
- 屏蔽词；
- 地区前缀搜索；
- 用户去重、页间续接、检查点；
- 多任务顺序执行；
- 本地任务记录与详情；
- 后端任务领取和状态回传；
- 远程任务开关；
- 基础登录与授权/心跳框架。

### 6.2 仍需持续修复

- 用户列表翻页后重复用户与漏用户问题仍需真机稳定验收。
- 用户名称提取仍可能受抖音节点结构变化影响。
- 账号稳定标识优先级应为：
  1. 抖音号 / handle；
  2. 用户名 + 公司或账号类型；
  3. 用户主页信息组合；
  4. 仅在必要时 OCR 辅助。
- 用户主页名称不要误取：
  - “搜索”；
  - “筛选，按钮”；
  - “背景图片”；
  - “用户头像”；
  - “求更新”；
  - ViewId；
  - 控制按钮文本。
- 主页名称兜底应该优先读取头像右侧、最大字体的账号名称。
- 任务完成、失败、人工暂停需要准确区分；不应在已完成后显示“需要人工处理”。
- 任务结果卡片：
  - 中文说明；
  - 内容完整换行；
  - 紧凑布局；
  - 不显示英文内部状态为面向用户的说明；
  - 结果记录不可重复。

## 7. App UI 与业务功能需求

### 7.1 底部导航

当前目标结构：

```
首页 | 待办 | 记录 | 设置
```

- 原“任务”改为“首页”。
- “远程任务”默认隐藏，仅在设置页开关开启后展示。
- “待办”独立为底部页面。
- 评论私信入口在首页“功能”区域。
- B 端私信入口在首页“功能”区域。

### 7.2 首页

首页不应再像 Debug 页面，应为正常产品化界面。

顶部：

- 问候语；
- “总览”；
- 显示：
  - `xxx 个任务已运行`
  - `xxx 个客户已发送`
- 数字带蓝色或主题色强调和加粗，描述文字不加粗。
- 右侧为“任务进展”：
  - 标题高度、字号、内容间距与左侧一致；
  - 进度圆环线条较细；
  - 百分比显示在圆环中心；
  - 无任务时显示 100%。

功能区：

- 标题为“功能”；
- 两张入口卡片：
  - `B端私信`
  - `评论私信`
- 评论私信当前逐步接入真实逻辑。

首页下方：

- 显示今日待办任务；
- 使用统一蓝色、简洁列表样式；
- 列表项无厚重卡片边框和背景；
- 右侧使用复选框；
- 与待办页风格一致。

### 7.3 待办页

- 顶部仅保留“待办任务”标题；
- 移除多余提示语和“可多选”说明；
- 列表项无边框、无重背景；
- 复选框在右侧；
- 页面底部固定“开始任务”按钮；
- 不显示图标；
- 选中多个任务后，按队列逐个执行。

### 7.4 新建 B 端私信任务页

- 顶部仅显示简洁服务状态，如“自动化服务正常”与图标。
- 缩小整体高度、字号、间距和输入框内边距。
- 任务名称非必填：
  - 为空时自动使用“搜索词 + 时间”生成。
- 预设搜索词：
  - 浅灰背景、深色文字；
  - 采用换行流式布局；
  - 不强制一行两个；
  - 左右内边距尽可能小。
- 选择预设搜索词后应填入自定义搜索词输入框；切换时替换。
- 筛选条件同样支持预设词与输入框同步。
- 筛选预设词至少包括：
  - 厂
  - 公司
- 移除：
  - 最终搜索词预览；
  - 保存配置按钮；
  - 清除配置按钮；
  - 返回任务工作台按钮。
- 主操作：
  - `立即开始`
  - `保存`
- 保存后回到首页，进入待办任务列表。

### 7.5 多任务队列

- 用户可保存多个任务。
- 待办页中勾选多个任务后点击“开始任务”。
- 队列按顺序串行执行：
  - 前一任务完成、失败或达到上限后；
  - 自动进入下一任务；
  - 全部完成后回 App 记录页。
- 任务队列、运行状态、检查点、结果记录应抽象为通用模块，供 B 端私信和评论私信复用。

## 8. 记录与任务详情需求

- 任务跑完，无论成功、部分失败还是达到上限，都应回到 App 并可进入记录页。
- 每条记录可打开详情页。
- 详情需展示：
  - 用户名称；
  - 稳定账号标识；
  - 开始时间；
  - 结束时间；
  - 发送模式；
  - 页面结果；
  - 成功、失败、跳过状态；
  - 面向普通用户的中文说明；
  - 失败原因；
  - 是否命中屏蔽词；
  - 是否重复用户。
- 账号名称、账号信息不可用 `..` 截断。
- 描述允许换行但整体卡片保持紧凑。
- 内部枚举如 `MESSAGE_EMPTY_REJECTED` 应翻译为用户可理解的中文页面说明。

## 9. 后端与远程任务

后端项目此前位于：

```
/Users/mac/Documents/abb/all/project202608plus/获客系统/AutomationSearchPost-FastapiAdmin
```

GitHub 仓库：

- Android：`https://github.com/PercyJon/AutomationSearchPostApp`
- 后端：`https://github.com/PercyJon/AutomationSearchPostAdmin`

后端功能目标：

- 管理员登录；
- App 登录；
- License / 设备绑定；
- 心跳验证；
- 远程任务创建、列表、领取、状态更新；
- 任务检查点；
- 用户处理结果上报；
- 任务详情查询；
- 搜索词预设和筛选词预设接口；
- 版本变更记录与 README 更新。

注意：

- Token、数据库密码、Redis 密码、ngrok 地址均不应硬编码进仓库。
- 新会话中需要重新获取当前 ngrok 地址和有效登录 Token。
- 每轮后端或客户端改动都应更新变更文档后再推送。

## 10. 当前已知 P0 卡点

### 10.1 调试任务没有真正触发

现象：

- 使用调试 Intent 打开 App 后可进入相关页面；
- 无障碍服务已经绑定；
- 但没有出现真正的任务启动日志，例如 `poc_started`；
- 抖音没有被拉起或没有进入自动化。

初步原因：

- `HomeScreen.kt` 中 `autoStartP0` 依赖 `canRun`；
- `canRun` 依赖 `serviceConnected`；
- Compose 初始化和无障碍连接状态存在竞态，导致自动启动错过时机。

必须修复：

- 自动调试启动应等待服务连接后再发送任务；
- 或实现可靠的服务侧调试入口；
- 失败时应在 UI/日志中明确说明；
- 不应静默停留在 App。

### 10.2 启动时抖音不在首页

现象：

- 抖音可能保留在上次用户主页；
- 自动化尝试从错误页面继续，造成卡住或误操作。

必须修复：

- 启动前必须归一化回抖音首页；
- 有节点时使用页面识别后退；
- 无节点时执行有限盲后退；
- 后退次数必须有上限；
- 不允许无限循环。

### 10.3 评论区结构误识别

历史错误包括：

- 将顶部位置卡、地址、评论数量识别为评论；
- 将“踩，已选中”“赞0，未选中”等控件文本识别为用户；
- 点击评论文本或其他区域后弹出回复键盘；
- 从首屏最后一个评论开始，而不是第一条有效评论；
- 未进入评论用户主页就直接记录“私信入口不可用”。

必须修复：

- 使用评论行结构识别；
- 头像必须与用户名、评论正文、元信息属于同一行；
- 只点击头像；
- 只处理第一条完整有效评论；
- 若无法确认，记录“评论结构无法确认”并停止本次 P0，不点击猜测区域。

### 10.4 P0 当前状态

P0 尚未完成一次可验证的真实闭环。此前的具体问题与未验收项已纳入本文件，后续应先完成这一项，再扩展到 5/10/20 条评论回归。 

## 11. P0 推荐执行顺序

新智能体接手后，请严格按以下顺序推进：

1. 确认 Android 工程路径、Git 状态和未提交变更。
2. 完成 `scheduleInitialObservation()` 的无节点有限后退恢复。
3. 修复 P0 调试自动启动与 `serviceConnected` 竞态。
4. 本地构建、单元测试、Lint。
5. 用户确认手机已连接后：
   - 检查 ADB；
   - 安装 APK；
   - 检查无障碍服务；
   - 启动固定 P0 回归。
6. 执行一次：
   - `designer`
   - 第一个用户
   - 1 个视频
   - 首条有效评论
   - 空格模拟发送
   - 返回评论区。
7. 仅在失败或异常时抓取诊断日志、必要截图。
8. 输出：
   - 每一步实际到达的页面；
   - 成功/失败原因；
   - 是否已完成闭环；
   - 对应日志；
   - 后续最小修复项。
9. P0 闭环成功后，再做：
   - 5 条评论；
   - 10 条评论；
   - 20 条评论；
   - 匹配词过滤；
   - 直播、私密、无作品、置顶视频、0 评论等分支回归。

## 12. 代码与提交规范

- 不覆盖用户已有未提交改动。
- 每轮改动后：
  - 构建；
  - 测试；
  - 更新变更记录；
  - 提交；
  - 推送至对应 GitHub 仓库。
- README 或独立 `CHANGELOG.md` 必须记录：
  - 日期；
  - 版本；
  - 修改内容；
  - 已验证项；
  - 未解决问题；
  - 已知限制。
- 不在 Git、日志、文档中写入真实密码、Token 或私密地址。

## 13. 当前阶段的最终优先级

```
P0  评论私信：designer / 1 视频 / 首条评论 / 空格模拟，跑通一次真实闭环
P1  评论私信：5、10、20 条有界真机回归
P2  评论筛选词、多视频、置顶跳过、当前主页模式
P3  B 端私信稳定性回归：翻页、去重、名称、异常恢复
P4  后端远程任务、License、心跳、预设词接口完善
P5  UI 产品化收尾、记录详情、统计、设置与发布准备
```

这份文档以当前 P0 闭环为第一优先级，新的开发者可直接从“第 11 节”开始接手。