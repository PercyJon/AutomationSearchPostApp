# Android 自动化项目开发规范

---

# 1. 开发与功能逻辑修复要求

开始修复前，先记录：

- 当前错误现象
- 稳定复现步骤
- 当前预期行为
- 实际行为差异

每一轮问题修复只允许：

1. 提出一个根因假设；
2. 修改最小范围；
3. 运行相同复现步骤；
4. 记录结果：
   - 改善
   - 无变化
   - 变差

禁止：

- 同时修改多个未知因素；
- 未验证直接继续扩大修改范围；
- 因为测试失败而进行无目的重构。

---

# 2. 问题止损机制

如果连续五轮没有解决同一个问题：

立即触发止损：

- 不进行第 6 次代码修改；
- 列出五轮修改分别做了什么；
- 列出日志、报错和已经排除的原因；
- 检查是否已经偏离最初问题；
- 给出最多 3 个剩余假设；
- 说明下一步最小诊断动作；
- 等待确认后再继续。

---

# 3. Android 几何尺寸 dp 归一化约束（P0）

所有新增 Android 几何尺寸逻辑，必须经过 dp 归一化检查。

## 3.1 禁止硬编码绝对像素

禁止新增：

- 固定 px 高度；
- 固定 px 宽度；
- 固定 px 距离阈值；
- 固定 px 行高判断。

禁止示例：

```kotlin
private const val MIN_ROW_HEIGHT = 180
private const val MAX_ROW_HEIGHT = 400
```

必须使用：

```kotlin
dp(value) = value * density
```

或者根据：

- 屏幕尺寸比例；
- 可用区域比例；

动态计算。

---

# 4. dp归一化与坐标归一化严格区分

项目存在两套不同归一化体系。

## 4.1 dp归一化

用途：

Android 尺寸和几何判断：

包括：

- OCR区域尺寸；
- 用户列表行高度；
- 控件距离；
- 页面元素尺寸阈值。

规则：

```
dp × displayMetrics.density
```

---

## 4.2 屏幕比例归一化

用途：

点击和手势坐标。

例如：

- NormalizedPoint
- NormalizedRect

范围：

```
0 ~ 1
```

用于：

- 点击位置；
- 滑动路径。

禁止：

将 dp 尺寸逻辑与坐标比例逻辑混用。

---

# 5. 当前待整改像素常量清单

以下历史代码必须逐步整改：

## StructuralUserRowDetector

当前：

```
MIN_ROW_HEIGHT = 180
MAX_ROW_HEIGHT = 400
```

要求：

改为 dp 或比例计算。

---

## DouyinNavigationController

当前：

```
USER_ROW_MIN_HEIGHT = 180
USER_ROW_MAX_HEIGHT = 400
```

要求：

改为动态尺寸。

---

## OcrUserResultRowDetector

当前：

```
MIN_ROW_HEIGHT_PX = 160
MAX_ROW_HEIGHT_PX = 440
```

要求：

移除 PX 命名和固定像素判断。

---

## GestureEngine

当前：

```
TAP_DURATION_MS = 60L
SWIPE_DURATION_MS = 260L
```

要求：

确认是否属于设备相关固定值。

如果属于设备差异因素：

必须抽象成配置或动态策略。

---

# 6. 自动化安全规则

所有自动点击逻辑：

必须优先：

1. Accessibility 节点；
2. OCR识别结果；
3. 几何校验；
4. 最后才允许安全兜底。

禁止：

- 直接依赖固定坐标；
- 未确认页面状态进行点击；
- 可能误触点赞、地址、AI解析等区域。

---

# 7. OCR识别规则

OCR结果不能直接作为点击依据。

必须：

OCR识别：

↓

几何验证：

↓

页面状态确认：

↓

执行动作

---

# 8. 真机验证规则

涉及以下功能：

- Accessibility流程；
- 页面跳转；
- 点击行为；
- 手势行为；
- 状态恢复；

必须进行真机验证。

记录：

- 设备型号；
- 操作路径；
- 日志；
- 成功/失败结果。

## 8.1 真机任务启动与监视

开始测试任务前：

1. 使用 `adb shell am force-stop com.ss.android.ugc.aweme` 杀掉抖音进程；
2. 再启动本次任务，保证从全新正常流程开始，不复用上一次残留页面。

任务启动后：

1. 每 30 秒轮询一次日志或任务状态；
2. 一旦出现成功、失败或安全暂停（`COMPLETED` / `FAILED` / `poc_paused_for_manual_handoff`），立刻接管诊断或复盘；
3. 禁止在终态已经出现后继续空等。

---

# 9. 提交规范

每完成一个阶段：

必须输出：

1. 修改内容；
2. 修改文件；
3. 测试结果；
4. 真机验证结果；
5. 新增几何常量检查结果；
6. dp归一化检查结果；
7. 下一步计划。

---

# 10. 开发原则

优先：

- 小步修改；
- 可验证修改；
- 保持已有稳定链路。

禁止：

- 为方便开发破坏已有安全策略；
- 未验证的大范围重构；
- 使用短期有效但设备不兼容的方案。
