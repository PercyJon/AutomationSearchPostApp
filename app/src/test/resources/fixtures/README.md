# M3-K 页面回归样本

此目录只允许提交脱敏的节点/布局摘要和不含账号、头像、私信内容的截图样本。真机采样时保留：

- 页面类型与设备分辨率；
- 节点 class、contentDescription、bounds 和层级路径；
- OCR 区域与识别结果的脱敏标签；
- 预期动作与 post-condition。

不得提交真实用户昵称、抖音号、头像或对话内容。当前首个样本由单元测试覆盖：用户列表中存在“找不到想要的账号？告诉我们”辅助行时，仍保持 USER_RESULTS；只有没有真实用户行时才作为结束候选。
